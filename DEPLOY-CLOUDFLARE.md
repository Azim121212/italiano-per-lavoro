# 🚀 Деплой на Cloudflare Pages

## Быстрый деплой

### Способ 1: Через скрипт (рекомендуется)

```bash
./deploy-cloudflare.sh
```

Скрипт автоматически:
- ✅ Проверит наличие Wrangler CLI
- ✅ Авторизует вас в Cloudflare (если нужно)
- ✅ Подготовит файлы для деплоя
- ✅ Загрузит сайт на Cloudflare Pages

### Способ 2: Через Wrangler CLI вручную

```bash
# Установка Wrangler (если еще не установлен)
npm install -g wrangler

# Авторизация
wrangler login

# Деплой
wrangler pages deploy . --project-name=ilearningedelweiss
```

### Способ 3: Через веб-интерфейс Cloudflare

1. Перейдите на https://dash.cloudflare.com/workers-and-pages
2. Нажмите **"Create application"** → **"Pages"** → **"Upload assets"**
3. Название проекта: `ilearningedelweiss`
4. Загрузите все файлы проекта (или создайте ZIP архив)
5. Нажмите **"Deploy site"**

## 📋 Требования

- **Node.js** и **npm** (для Wrangler CLI)
- **Аккаунт Cloudflare** с доменом `ilearningedelweiss.uk`
- **Wrangler CLI** (установится автоматически через скрипт)

## 🔧 Настройка домена

После успешного деплоя:

1. Перейдите в проект: https://dash.cloudflare.com/workers-and-pages → `ilearningedelweiss`
2. Откройте вкладку **"Custom domains"**
3. Нажмите **"Add custom domain"**
4. Введите: `ilearningedelweiss.uk`
5. Cloudflare автоматически настроит DNS записи
6. Повторите для `www.ilearningedelweiss.uk` (опционально)

### Альтернативный способ через DNS:

Если не можете найти "Custom domains", настройте вручную:

1. Перейдите в **DNS** → **Records** вашего домена
2. Добавьте CNAME запись:
   - **Name**: `@` (или оставьте пустым)
   - **Target**: `ilearningedelweiss.pages.dev`
   - **Proxy status**: Проксированный (оранжевое облако)
3. Для www:
   - **Name**: `www`
   - **Target**: `ilearningedelweiss.pages.dev`
   - **Proxy status**: Проксированный

## 📁 Структура файлов для деплоя

Все файлы из корневой директории будут загружены, кроме:
- `.git/` и `.gitignore`
- `node_modules/`
- `*.sh`, `*.bat` (скрипты)
- `*.md` (документация)
- `*.zip` (архивы)
- `ssh-key.txt` (секретные файлы)

## ✅ Проверка после деплоя

После деплоя проверьте:

1. ✅ Сайт доступен: https://ilearningedelweiss.pages.dev
2. ✅ Главная страница загружается: `/`
3. ✅ Админ-панель работает: `/admin/`
4. ✅ Платформа работает: `/platform/`
5. ✅ Кастомный домен настроен: `ilearningedelweiss.uk`

## 🔄 Обновление сайта

Для обновления сайта просто запустите скрипт деплоя снова:

```bash
./deploy-cloudflare.sh
```

Или через Wrangler:

```bash
wrangler pages deploy . --project-name=ilearningedelweiss
```

## ⚠️ Важные замечания

- **HTTPS** включается автоматически
- **Кеширование**: Cloudflare кеширует статические файлы автоматически
- **Редиректы**: Файл `_redirects` настроен для правильной работы SPA
- **Локальное хранилище**: Данные в `localStorage` будут храниться локально в браузере каждого пользователя

## 🐛 Решение проблем

### Ошибка авторизации
```bash
wrangler login
```

### Ошибка "Project not found"
Создайте проект вручную через веб-интерфейс Cloudflare Pages

### Домен не работает
1. Проверьте DNS записи
2. Убедитесь, что домен подключен в Cloudflare Pages
3. Подождите 1-2 минуты для распространения DNS

### Файлы не обновляются
1. Очистите кеш браузера (Ctrl+Shift+R)
2. Проверьте, что деплой прошел успешно
3. Подождите несколько минут для обновления CDN

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Cloudflare Dashboard
2. Откройте консоль браузера (F12) для отладки
3. Проверьте файл `_redirects` на наличие правильных редиректов

