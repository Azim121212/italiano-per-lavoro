# Пошаговая инструкция: Деплой на Cloudflare Pages

## ✅ Шаг 1: Домен уже добавлен в Cloudflare (готово!)

## 📍 Шаг 2: Создание проекта в Cloudflare Pages

### Где найти Cloudflare Pages:

1. **В левом меню Cloudflare Dashboard** найдите раздел **"Workers & Pages"**
   - Это отдельный раздел, не внутри вашего домена
   - Может быть в верхней части меню или в разделе "Products"

2. **Альтернативный путь:**
   - В верхней части страницы найдите выпадающий список с вашим доменом
   - Нажмите на него и выберите **"Workers & Pages"** или **"All Products"**
   - Затем выберите **"Workers & Pages"**

3. **Прямая ссылка:**
   - https://dash.cloudflare.com/workers-and-pages

### Создание проекта:

1. На странице **"Workers & Pages"** нажмите большую синюю кнопку **"Create application"**
2. Выберите вкладку **"Pages"** (не Workers)
3. Выберите **"Upload assets"** (загрузка файлов)
4. Название проекта: `ilearningedelweiss`
5. Загрузите файл `site-deploy.zip` (находится в папке проекта)
6. Нажмите **"Deploy site"**

## 🔗 Шаг 3: Подключение домена к Pages проекту

### После успешного деплоя:

1. На странице вашего проекта **ilearningedelweiss** (в разделе Workers & Pages)
2. Откройте вкладку **"Custom domains"** (в верхнем меню проекта)
3. Нажмите кнопку **"Set up a custom domain"** или **"Add custom domain"**
4. Введите: `ilearningedelweiss.uk`
5. Нажмите **"Continue"**
6. Cloudflare автоматически настроит DNS записи
7. Повторите для `www.ilearningedelweiss.uk` (опционально)

### Если кнопки "Custom domains" нет:

1. Убедитесь, что проект успешно задеплоен (статус "Active")
2. Проверьте, что вы находитесь на странице проекта, а не в общем списке
3. Вкладка "Custom domains" находится в верхнем меню проекта (рядом с "Overview", "Deployments" и т.д.)

## 📝 Альтернативный способ через DNS:

Если не можете найти "Custom domains", можно настроить вручную через DNS:

1. Перейдите в ваш домен: **DNS** → **Records**
2. Добавьте CNAME запись:
   - **Name**: `@` (или оставьте пустым для корневого домена)
   - **Target**: `ilearningedelweiss.pages.dev` (URL вашего Pages проекта)
   - **Proxy status**: Проксированный (оранжевое облако)
3. Для www:
   - **Name**: `www`
   - **Target**: `ilearningedelweiss.pages.dev`
   - **Proxy status**: Проксированный

## ⚠️ Важно:

- После подключения домена подождите 1-2 минуты
- HTTPS включится автоматически
- Если домен не работает сразу, проверьте DNS записи

